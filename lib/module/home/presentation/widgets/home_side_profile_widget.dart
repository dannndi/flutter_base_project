import 'package:flutter/material.dart';

class HomeSideProfileWidget extends StatelessWidget {
  const HomeSideProfileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
